version: '3.8'
services:
  api:
    build: ./server
    ports:
      - '8080:8080'
    env_file:
      - ./server/.env
    depends_on:
      - mongo
  mongo:
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: